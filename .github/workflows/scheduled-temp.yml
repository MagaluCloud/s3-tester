name: Scheduled test runs

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

env:
  PROFILES: ${{ secrets.temp_profile }}

jobs:
  TEMP_TEST:
    runs-on: ubuntu-latest
    container: ghcr.io/marmotitude/s3-tester:tests
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: "Profiles: ${{ vars.TEMP_PROFILES }}; Clients: ${{ vars.TEMP_CLIENTS }}; Tests: ${{ vars.CRON_TESTS_1 }};"
        if: github.event.schedule == '0 */6 * * *'
        run: |
          echo copying specs from the workspace volume to the image workspace...
          cp $GITHUB_WORKSPACE/spec/*.sh /app/spec/.
          cd /app
          mgc --version
          sha256sum `which mgc`
          ./entrypoint.sh test.sh --profiles "${{ vars.TEMP_PROFILES }}" --tests "${{ vars.CRON_TESTS_1 }}" --clients "${{ vars.TEMP_CLIENTS }}" -- --env NUMBER_OF_WAITS=${{ env.NUMBER_OF_WAITS }} --env SKIP_KNOWN_ISSUES=${{ env.SKIP_KNOWN_ISSUES }} --color
